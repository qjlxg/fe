name: 07-fund_warning_report_v6.0

on:
  push:
    paths:
      - 'analyzer_V6.py'  # 确保匹配你保存的文件名
      - '.github/workflows/**'
  schedule:
    # 每天北京时间 15:15 运行（收盘后分析）
    - cron: '15 7 * * *'
  workflow_dispatch: 

jobs:
  analyze_funds:
    env:
      TZ: Asia/Shanghai
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # 核心更新：必须安装 akshare 以支持自动数据抓取
          pip install pandas numpy akshare

      - name: Run fund analysis script
        id: analysis
        # 确保文件名与你仓库里的一致
        run: python analyzer_V6.py

      - name: Commit and push changes
        if: success()
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          
          # 允许添加新生成的数据文件夹和报告
          git add fund_data/*.csv || true
          git add *.md || true
          git add history_signals.csv || true
          
          if [ -z "$(git status --porcelain)" ]; then
            echo "No changes detected. Skipping commit."
            exit 0
          fi
          
          git commit -m "📈 [V6.0] 自动行情同步与决策报告: $(date +'%Y-%m-%d %H:%M')"
          
          # 这里的 rebase 是为了防止多个 workflow 并发冲突
          git pull --rebase origin main
          git push origin main
