name: 🚀 本地增强版实盘看板

on:
  push:
    paths:
      - 'analyzer_V9.py'
  schedule:
    - cron: '55 6 * * *' # UTC 6:55 = 北京时间 14:55
  workflow_dispatch: 

jobs:
  run_analysis:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: 🛠️ 依赖安装
        run: pip install pandas numpy
      - name: 📈 执行实时分析
        run: python analyzer_V9.py
      - name: 💾 推送结果
        run: |
          git config --global user.name "Quant-Bot"
          git config --global user.email "bot@quant.com"
          git add README.md
          git commit -m "📊 信号生成: $(date +'%Y-%m-%d %H:%M')" || echo "No changes"
          git push
