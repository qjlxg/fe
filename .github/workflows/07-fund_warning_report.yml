name: 07-fund_warning_report_v5.8

on:
  push:
    paths:
      - 'analyzer_V5.py'  # 确保这里匹配你的最新文件名
      - '.github/workflows/**'
      - 'fund_data/**'
  schedule:
    # 每天北京时间 15:15 运行（A股收盘后立即分析）
    # UTC 时间 07:15
    - cron: '15 7 * * *'
  workflow_dispatch: # 允许手动点按钮运行

jobs:
  analyze_funds:
    env:
      TZ: Asia/Shanghai
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy

      - name: Run fund analysis script
        id: analysis
        # 注意：这里改为你最新的文件名
        run: python analyzer_V5.py

      - name: Commit and push changes
        if: success()
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          
          # 1. 添加所有类型的报告
          git add Trading_Decision_Report_*.md || true
          git add Strategy_Pro_Report_*.md || true
          
          # 2. 关键：添加复盘日志文件，这样历史记录才不会丢
          git add history_signals.csv || true
          
          # 检查是否有文件变动
          if [ -z "$(git status --porcelain)" ]; then
            echo "No changes detected. Skipping commit."
            exit 0
          fi
          
          git commit -m "📈 [V5.8] 自动化决策报告与日志同步: $(date +'%Y-%m-%d %H:%M')"
          
          # 处理并发冲突
          git pull --rebase origin main
          git push origin main
