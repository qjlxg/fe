name: 🚀 V10
on:
  schedule:
    - cron: '45 7 * * *' # 北京时间 15:45
  workflow_dispatch: 

jobs:
  run_bot:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 配置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 🛠️ 安装增强依赖
        run: |
          pip install pandas numpy akshare

      - name: 📈 运行 V10 核心系统
        run: python analyzer_V9.py

      - name: 💾 同步分析结果
        run: |
          git config --global user.name "Sky-Pivot-Bot"
          git config --global user.email "bot@quant.com"
          git add README.md signal_performance_tracker.csv portfolio.csv
          if [ -z "$(git status --porcelain)" ]; then
            echo "No change"
            exit 0
          fi
          git commit -m "📊 V10.0 智能看板更新: $(date +'%Y-%m-%d %H:%M')"
          git push origin main
