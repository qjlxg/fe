name: 🚀 天枢北京时间同步版

on:
  schedule:
    # 每天北京时间 15:45 运行分析
    - cron: '45 7 * * *'
  workflow_dispatch: 

jobs:
  analyze:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 配置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 🛠️ 安装依赖库
        run: pip install pandas numpy akshare

      - name: 📈 运行分析 (北京时间转换)
        run: python analyzer_V9.py

      - name: 💾 推送结果回目录
        run: |
          git config --global user.name "Sky-Pivot-Bot"
          git config --global user.email "bot@quant.com"
          git add README.md portfolio.csv signal_performance_tracker.csv
          git commit -m "📊 更新看板(北京时间): $(date +'%Y-%m-%d %H:%M')" || echo "No changes"
          git push
