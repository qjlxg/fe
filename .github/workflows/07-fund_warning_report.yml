name: 🚀 天枢本地增强版

on:
  push:
    paths:
      - 'analyzer_V9.py'
      - '*.csv'  # 包含沪深列表更新时运行
  schedule:
    - cron: '50 7 * * *' # 北京时间 15:50
  workflow_dispatch: 

jobs:
  run_analysis:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 📥 1. 拉取仓库
        uses: actions/checkout@v4

      - name: 🐍 2. Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 🛠️ 3. 依赖
        run: pip install pandas numpy openpyxl

      - name: 📈 4. 执行本地化分析
        run: python analyzer_V9.py

      - name: 💾 5. 推送结果
        run: |
          git config --global user.name "Sky-Pivot-Bot"
          git config --global user.email "bot@quant.com"
          git add README.md signal_performance_tracker.csv
          git commit -m "📊 终极本地看板更新: $(date +'%Y-%m-%d %H:%M')" || echo "No changes"
          git push
