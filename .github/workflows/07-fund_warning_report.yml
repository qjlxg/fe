name: 🚀 本地增强版实盘看板

on:
  push:
    paths:
      - 'analyzer_V9.py'
      - '.github/workflows/07-fund_warning_report.yml'  
  schedule:
    - cron: '55 6 * * *' # 北京时间 14:55 运行 (UTC 6:55)
  workflow_dispatch: 

jobs:
  run_analysis:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 📥 1. 拉取仓库
        uses: actions/checkout@v4

      - name: 🐍 2. 初始化 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 🛠️ 3. 安装量化依赖
        run: pip install pandas numpy openpyxl

      - name: 📈 4. 执行多维评估逻辑
        run: python analyzer_V9.py

      - name: 💾 5. 推送看板与绩效数据
        run: |
          git config --global user.name "Quant-Bot"
          git config --global user.email "bot@quant.com"
          # 检查是否有文件更新，避免提交空记录
          git add README.md
          git commit -m "📊 实盘信号自动更新: $(date +'%Y-%m-%d %H:%M')" || echo "No changes"
          git push
